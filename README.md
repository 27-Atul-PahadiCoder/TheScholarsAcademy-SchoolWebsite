# The Scholars Academy

This is a code bundle for The Scholars Academy.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
# TheScholarsAcademy-School-Website
